# Define a number
x = 1

# Print the binary representation of x
print(f"x = {x} in binary: {bin(x)}")

# Multiply x by 2 using bitwise left shift
x_shifted = x << 2

# Print the binary representation of the shifted value
print(f"x << 1 = {x_shifted} in binary: {bin(x_shifted)}")